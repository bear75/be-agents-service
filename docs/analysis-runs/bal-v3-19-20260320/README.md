# Bal scenario analysis (v3/19) — 2026-03-20

**Input:** `recurring-visits/.../v3/19/bal/export-field-service-routing-v1-20228091-2732-4a2f-bee2-c43c234b81ba-input.json`  
**Output:** `recurring-visits/.../v3/19/bal/export-field-service-routing-20228091-2732-4a2f-bee2-c43c234b81ba-output.json`

Generated by:

```bash
./scripts/analysis/run_full_fsr_analysis.sh \
  "<path-to-input.json>" \
  "<path-to-output.json>" \
  docs/analysis-runs/bal-v3-19-20260320
```

## Artefakter

| Fil | Innehåll |
|-----|----------|
| `metrics.txt` | `metrics.py` — KPI, 158 unassigned, field efficiency ~78 % |
| `unassigned.csv` | Per TW-rad: `issue` = supply \| config, datum, bucket dag/kväll |
| `analyze_unassigned.txt` | Samma som konsollpuff för analyze_unassigned |
| `dependency_report.json` | `analyze_dependency_feasibility.py --all` |
| `supply_demand.md` | Efterfrågan vs kapacitet per bucket |
| `continuity.csv` | Kontinuitet per klient |

## Sammanfattning (158 unika unassigned besök, 245 TW-rader)

- **Supply (ingen skiftöverlap mot fönster):** 14 TW-rader → **alla 14 är klient `H031`**, besök `H031_2026-03-06_41` (rörligt fönster 14:00–14:54 över många datum; inget skift täcker enligt modellen).
- **Config (minst ett skift överlappar):** 231 TW-rader → solver/routing/kapacitet/beroenden/vikter.
- **Tunga klient-prefix i CSV (flera TW-rader):** `H301`, `H299`, `H035`, `H038`, `H026`, `H362`, `H034` — återkommer ofta → typiska flaskhalsar (dubbelpersonal/grupp, snäva fönster, kväll).
- **verify_flex:** 80 besök med 0 flex (`minStartTime==maxStartTime`) — svårare för solver att flytta.

## Åtgärder (kopplat till din checklista)

1. **Supply (~14):** Lägg skift som täcker **14:00–15:00** (eller bredare) på relevanta fordon/zoner så `H031_…_41` kan placeras, eller justera besökets TW i källan.
2. **Config:** Längre `spentLimit`, sänk travel-vikt i solver config, ev. `expand_supply_shifts.py` för forskningskörning.
3. **DB-export:** Exportera Timefold-input **efter** `fixInfeasibleDependencies` / färdig seed så beroenden matchar dashboard — denna fil är en **export**; validera mot aktuell DB om du jämför med Caire.

## Dependency feasibility (Python, värsta-fall)

64 klassade som “infeasible”, 14 “tight” — se `dependency_report.json`. Tolkning: del kan vara pessimistisk vs solver; resterande kan kräva TW/delay-justering i data.
