# Input comparison: cece06c0 (0 unassigned) vs 16f53762 (2w pipeline)

## Summary

| Run | Input | Vehicles | Shifts | Visits | Visit groups | Identical to cece06c0? |
|-----|--------|----------|--------|--------|--------------|-------------------------|
| **cece06c0** | `9march/xtra-shifts/export-field-service-routing-v1-cece06c0-...-input.json` | 46 | **448** | 3,457 | 116 (240 in groups) | — |
| **16f53762** | `fsr_input_81_2w.json` (pipeline 2w) | 46 | **544** | 3,457 | 116 (240 in groups) | **No** (different shift count) |
| **cece06c0 + dusch fix** | `9march/xtra-shifts/cece06c0-with-dusch-fix-input.json` | 46 | 448 | 3,457 | 116 | **INVALID** – precedingVisit H035_r47_5+ do not exist (cece06c0 only has r47_1..4) |

- **16f53762** is from the Attendo CSV pipeline with `--end-date 2026-03-15` and `--no-supplementary-vehicles`. It has **544 shifts** (same 2-week window, same 46 vehicles, but more shifts per vehicle than the cece06c0 input). The only intentional change vs a “vanilla” 2w run is the **dusch fix** in the script (Morgon→Dusch PT0M for H035).
- **cece06c0** used an input with **448 shifts** (same window, same fleet size). The two unassigned visits were **H035_r53_5** and **H035_r54_5** (H035 dusch visit group).
- To get a run that is **identical to cece06c0 except the dusch fix**, use **cece06c0-with-dusch-fix-input.json**: same 448 shifts, same visits/groups, with added `visitDependencies` so every H035_r53_* and H035_r54_* has `precedingVisit: H035_r47_*` (same occurrence index), `minDelay: PT0M`, `coordination: NONE`.

## Difference in shifts (448 vs 544)

- cece06c0 input: 46 vehicles × fewer shifts per vehicle = 448 total (likely from a different export or shift rule).
- Our pipeline 2w: 46 vehicles × more shifts per vehicle = 544 total.

So **16f53762 is not identical to cece06c0**; the only shared intent is “2-week window, same visits, and the dusch fix applied.”

## Validation before submit

**Always run** `python3 submit_to_timefold.py validate <input.json>` before submitting. The validator checks that every `visitDependencies[].precedingVisit` exists in `visits` or `visitGroups`. The failed run **2af137d8** (and c962d24e) used `cece06c0-with-dusch-fix-input.json`, which referenced H035_r47_5 … H035_r47_14; in the cece06c0 input only H035_r47_1..4 exist, so the solve failed.

## Valid 2w input with dusch fix (pipeline)

- **Input:** `fsr_input_81_2w.json` (generated by `attendo_4mars_to_fsr.py` with `--end-date 2026-03-15`, dusch fix in script).
- **Route plan ID:** **7ab3a11c-9ba5-449b-80a0-e792581399f4** (validated then submitted).
- 46 vehicles, 544 shifts, 3,457 visits, 116 groups. Same 2-week window; dusch fix uses morgon visit IDs that exist in this input.
