# Copy this file to ~/.config/caire/env and fill in your keys.
# The real file is OUTSIDE the repo (never commit secrets).
#
#   cp config/caire/env.example ~/.config/caire/env
#   chmod 600 ~/.config/caire/env
#   nano ~/.config/caire/env   # paste your keys
#
# Get Anthropic key: https://console.anthropic.com/settings/keys

export ANTHROPIC_API_KEY="paste-your-key-here"
export GITHUB_TOKEN="paste-your-token-here"
